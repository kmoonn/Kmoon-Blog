{{- $authorName := "" }}
{{- $authorEmail := "" }}
{{- with site.Params.author }}
  {{- if reflect.IsMap . }}
    {{- with .name }}
      {{- $authorName = . }}
    {{- end }}
    {{- with .email }}
      {{- $authorEmail = . }}
    {{- end }}
  {{- else }}
    {{- $authorName = . }}
  {{- end }}
{{- end }}
{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = .Site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
  {{- $pages = $pctx.RegularPages }}
{{- else }}
  {{- $pages = $pctx.Pages }}
{{- end }}
{{- $pages = where $pages "Params.hidden" "!=" true }}
{{- with site.Params.RSS.pageLimit }}
  {{- if ge . 1 }}
    {{- $pages = $pages | first . }}
  {{- end }}
{{- end }}
{{- $sortedPages := $pages.ByLastmod.Reverse }}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne .Title .Site.Title }}{{ with .Title }}in {{ . }} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo</generator>
    <language>{{ with .Site.Language.LanguageCode }}{{ . }}{{ else }}en-US{{ end }}</language>
    {{ with $authorEmail }}
      <managingEditor>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</managingEditor>
    {{ end }}
    {{ with $authorEmail }}
      <webMaster>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</webMaster>
    {{ end }}
    {{ with site.Params.copyright }}
    <copyright>{{ . }}</copyright>
    {{ end }}
    {{ if gt (len $sortedPages) 0 }}
    <lastBuildDate>{{ (index $sortedPages 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{ end }}
    {{ if and site.Params.RSS.followFeedId site.Params.RSS.followUserId }}
    <follow_challenge>
        <feedId>{{ site.Params.RSS.followFeedId }}</feedId>
        <userId>{{ site.Params.RSS.followUserId }}</userId>
    </follow_challenge>
    {{ end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range $pages }}
    <item>
      {{- $itemDate := .PublishDate }}
      {{- if $itemDate.IsZero }}
        {{- $itemDate = .Date }}
      {{- end }}
      {{- if $itemDate.IsZero }}
        {{- $itemDate = .Lastmod }}
      {{- end }}
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ $itemDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with $authorEmail }}<author>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</author>{{ end }}
      <guid>{{ .Permalink }}</guid>
      <description>{{ .Content | transform.XMLEscape | safeHTML }}</description>
    </item>
    {{- end }}
  </channel>
</rss>
